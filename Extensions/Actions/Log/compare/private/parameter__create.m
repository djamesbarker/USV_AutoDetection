function parameter = parameter__create(context)

% COMPARE - parameter__create

parameter = struct;

parameter.reference_log = '';

parameter.event_overlap_threshold = 0;

parameter.low_score = 0;
parameter.high_score = 10;
parameter.n_points = 10;




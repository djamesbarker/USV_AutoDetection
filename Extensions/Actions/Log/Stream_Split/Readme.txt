9/12/14

Streamsplit will allow many files to be loaded as a 'file stream'.  These file streams can be processed as a 'Batch' and then split into individual logs for each individual file by the stream-split command.


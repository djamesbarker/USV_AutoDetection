function ext = binomial

ext = extension_inherit(linear_base_ext);

ext.short_description = 'Binomial mask linear filter';

ext.category = {ext.category{:}, 'Blur', 'Statistical'};

% ext.version = '';

% ext.author = '';

% ext.email = '';

% ext.url = '';

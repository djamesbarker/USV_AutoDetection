function ext = highpass_ext

ext = extension_inherit(bandpass_ext);

ext.short_description = 'highpass linear filter';

% ext.category = {ext.category{:}};

% ext.version = '';

% ext.author = '';

% ext.email = '';

% ext.url = '';


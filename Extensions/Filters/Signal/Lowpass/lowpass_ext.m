function ext = lowpass_ext

ext = extension_inherit(bandpass_ext);

ext.short_description = 'lowpass signal filter';

% ext.category = {ext.category{:}};

% ext.version = '';

% ext.author = '';

% ext.email = '';

% ext.url = '';


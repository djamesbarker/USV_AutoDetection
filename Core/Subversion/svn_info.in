function info = svn_info

% NOTE: this function is generated by 'tsvn_replace' from 'svn_info.in'

%-----------------------------------------------------------------------

% Highest committed revision number

info.rev = $WCREV$;				

% Date of highest committed revision 

info.date = '$WCDATE$';			

% Update revision range 

info.rev_range = '$WCRANGE$';	

% Repository URL of the working copy 

info.url = '$WCURL$';			

%-----------------------------------------------------------------------
